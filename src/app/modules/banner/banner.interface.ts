export type TButton = {
  text: string;
  link: string;
};

export type TBanner = {
  bannerImage: string;
  isActive: boolean;
  link: string;
};

export type TSpecialBanner = {
  bannerImage: string;
  product: string;
  time: string;
};
